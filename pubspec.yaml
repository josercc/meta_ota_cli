name: meta_ota_cli
description: CLI for Meta Code Push — register releases, upload patches, promote, rollback
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.5.0
executables:
  meta_ota: meta_ota
dependencies:
  args: ^2.6.0
  crypto: ^3.0.6
  http: ^1.2.2
  path: ^1.9.1
  ota_protocol:
    path: packages/ota_protocol
dev_dependencies:
  lints: ^5.0.0
